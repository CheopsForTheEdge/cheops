package openstack


/* 	don't know if necessary, but this file should specify to Cheops how to
	recognize an openstack request I think
 	so we need the same functionality in each driver (and have a package drivers
	to specify which drivers are available and their endpoint?)
*/
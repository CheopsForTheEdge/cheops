Watch -r goimports -w . '&&' gofmt -w . '&&' go test '&&' go build

# cli
./cli exec --command='mkdir foo && cat {/etc/hostname} > foo/hostname' --sites='localhost&other' --local-logic=local --config=config.json

# server
MYFQDN=localhost ./cheops.com